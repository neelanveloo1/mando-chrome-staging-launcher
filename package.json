{
  "name": "mando-chrome-staging-launcher",
  "private": true,
  "version": "1.2.1",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-web.mjs",
    "test": "node --test tests/*.test.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "package": "node scripts/package.mjs"
  },
  "devDependencies": {
    "prettier": "3.6.2"
  },
  "engines": {
    "node": "22.x"
  }
}
